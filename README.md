# API-Atualizada
Foi desenvolvido um sistema para a Unimed utilizando a abordagem de código aberto (OpenCode). O front-end foi construído utilizando HTML e CSS, enquanto o backend foi implementado com Python, utilizando o framework Flask e a biblioteca Pandas.

# API - Updated
A system was developed for Unimed using the OpenCode approach. The front-end was built using HTML and CSS, while the back-end was implemented with Python, utilizing the Flask framework and the Pandas library.
